export class ProjectModel {
    constructor(public imgURL: string, public num: string , public name: string, public price: string,
         public status: string, public data: string, public join_num: number) {

    }
}
